# Local Python env for migration_orchestrator (created by repo start_upgrade.sh)
.venv/
__pycache__/
*.pyc

# dev-toolkit jar: fetched via toolkit-release.json (tools/fetch_jar.py),
# never vendored in git.
lib/*.jar
